## Setup

```bash
# Note: it will overwrite your existing $HOME/.tmux.conf, if any.
mkdir -p $HOME/.config
cd $HOME/.config && git clone git@github.com:dbobrenko/nvim.git && cd nvim && bash install.sh
```
